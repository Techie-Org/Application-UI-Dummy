/**
 * Homepage messages
 *
 * This contains all the text for GlobalHeader component
 */
import { defineMessages } from 'react-intl';

const scope = 'artistry.components.GlobalHeader';

export default defineMessages({
  logoAria: {
    id: `${scope}.logoAria`,
    defaultMessage: 'Artistry logo link to homepage',
  },
});
